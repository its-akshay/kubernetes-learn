Cluster       A group of machines managed as one unit
Node          A single machine (VM/physical) in the cluster
Pod           Smallest deployable unit — wraps 1+ containers
Deployment    Declares desired state: '3 replicas of my Go server'
Service       Stable network endpoint in front of a set of Pods
Namespace     Virtual cluster for isolating teams or environments
kubectl       CLI tool to interact with the Kubernetes API
YAML          manifest File that describes what you want K8s to create/maintain


===========2=========
Control Plane (Master)
• kube-apiserver — REST API front door; every kubectl command hits this.
• etcd — distributed key-value store, single source of truth for cluster state.
• kube-scheduler — picks which node a new Pod should run on.
• kube-controller-manager — runs controllers (ReplicaSet, Node, Endpoint...).
Worker Nodes
• kubelet — agent on every node; starts/stops containers as instructed.
• kube-proxy — maintains iptables rules for Service routing.
• Container Runtime — actually runs containers (containerd, CRI-O).

==========3===========
an installation setup is done 
we need docker kubectl and minikube(local cluster)


=======4===========
A Pod is one or more containers sharing a network namespace and storage volumes. In practice we never create
bare Pods; a Deployment manages them for us.
we create a pod.yaml


few commands
kubectl apply -f pod.yaml
kubectl get pods
kubectl describe pod demo-pod
kubectl logs demo-pod
kubectl exec -it demo-pod -- bash
kubectl delete pod demo-po

A Deployment ensures N copies of your Pod always run. It handles rollouts, rollbacks, and self-healing automatically.

we create a deployment.yaml
Deployment → ReplicaSet → Pods → Containers
Deployment
 ├── metadata (name)
 └── spec
      ├── replicas
      ├── selector
      └── template (Pod blueprint)
            ├── metadata (labels)
            └── spec (containers)




service.yaml
Pod IPs change on restart. A Service gives a stable DNS name + IP that load-balances across all matching Pods

configmap.yaml (it externalise configuration)
In Kubernetes, a ConfigMap stores non-sensitive configuration data (like environment variables or config files) separately from container images.


Minikube = Local Kubernetes environment for learning, testing, and development

kube-apiserver → entry point (all commands go here)
etcd → database storing cluster state
kube-scheduler → decides which node runs your pod
kube-controller-manager → keeps system in desired state
kube-proxy → networking (service routing)
coredns → DNS inside cluster
storage-provisioner → handles volumes


deleting pods will try to create replica again with new deployment yaml file

“NodePort isn’t directly accessible, so Minikube creates a tunnel to map service to localhost.”

if we curl we can see that hostname is changing each time:
1..5 | ForEach-Object { curl.exe http://127.0.0.1:54640; echo "`n" }


kubectl = CLI client
minikube = local Kubernetes cluster

service exposes 2 urls using minikube 
1 NodePort
2 TunnelUrl

Load balancing behavior
NOT strict round-robin
Can hit same pod multiple times

Client → Service → kube-proxy → Pods